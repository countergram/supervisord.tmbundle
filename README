TextMate bundle for supervisord.conf, used by the Supervisor process management daemon. Besides syntax highlighting, provides some snippets for common sections and a "Run" command that attempts to start Supervisor in an iTerm with the configuration file being edited.

INSTALLATION

cd ~/Library/Application\ Support/TextMate/Bundles/
git clone git://github.com/countergram/supervisord.tmbundle.git
